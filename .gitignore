# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
.expo/
.expo-shared/
.expo-export/
.expo-export-android/
.expo-export-ios/
.expo-web/
web-build/

# Env (NEVER commit secrets)
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Native (regenerated by expo prebuild)
ios/Pods/
android/.gradle/
android/build/
android/app/build/
santiagoways-app/ios/
santiagoways-app/android/

# Stray root-level prebuild artifacts — the real native projects and Expo
# config live inside santiagoways-app/. These are throwaway output from running
# `expo prebuild`/`expo run` at the monorepo root (namespaced com.anonymous.*).
/android/
/ios/
/app.json
.expo/prebuild/

# Aider tool artifacts
.aider*

# TypeScript incremental build info
*.tsbuildinfo

# Prisma local generated client
node_modules/.prisma/

# QR / temp
QR.png
*.tmp

# Claude Code workspace
.claude/
