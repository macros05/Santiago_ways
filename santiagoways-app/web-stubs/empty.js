// Web stub for native-only modules (maplibre, health, purchases). These are
// loaded via guarded require()/lazy import in the app, so an empty module makes
// the app fall back to its non-native behaviour on web.
module.exports = {};
module.exports.__esModule = true;
module.exports.default = {};
